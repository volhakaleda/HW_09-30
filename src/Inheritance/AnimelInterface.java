package Inheritance;


// the difference between abstract class & interface is that abstarct class can have properties; abstract & regular methods.
//Interface can have only abstract methods.
//they can't be instantiated(does not create new instances/objects).

public interface AnimelInterface {
    public abstract void move();

}
